node_modules/
.next/
dist/
coverage/
.turbo/
.env
.env.local
.env.dealer
*.log
# Кэш инкрементальной сборки TypeScript — машинный, шумит в каждом diff
*.tsbuildinfo
uploads/
# Исходники макетов: в продукт идут экспортированные ассеты, не PSD
*.psd
# Образцы прайсов дилера: загружаются через интерфейс, в репозитории не нужны
*.xlsx

# Локальные конфиги AI-агентов и редакторов — машинные, не для репозитория
.claude/
.codex/
.cursor/
.grok/
.impeccable/
.screenshots/
.vscode/
# только хуки агентов; workflows GitHub при появлении будут коммититься
.github/hooks/
